---
- name: Prepare
  hosts: all
  tasks:
    - name: Ensure prepare packages are present
      ansible.builtin.package:
        name:
          - shadow-utils
          - sudo
        state: present
...
