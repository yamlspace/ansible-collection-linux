---
- name: Prepare
  hosts: all
  tasks:
    - name: Ensure prepare packages are present
      ansible.builtin.package:
        name:
          - kmod
          - procps-ng
          - sudo
        state: present
...
